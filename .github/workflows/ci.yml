name: onPush

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Get Weather
    steps:
      - uses: actions/checkout@v2
      - name: Get Weather
        shell: bash
        run: chmod +x ./src/getWeather.sh && ./src/getWeather.sh